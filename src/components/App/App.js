import React from 'react';
import './App.css';
import { useState } from 'react';
import axios from 'axios';

function App() {
  // const [randomGif, setRandomGif] = useState('');

  // const handleClick= () => {
  //   axios.get('/random')
  //     .then(response => setRandomGif(response.data.data))
  //     .catch(error => console.log('error is', error))
  // }

  // console.log('random gif is', randomGif);

  // Renders the entire app on the DOM
  return (
    <div>
      {/* <header className="App-header">
        <h1>Random Giphy API</h1>
      </header>
      
      <button onClick={handleClick}>Random gif please!</button>
      <img src={randomGif} /> */}

    </div>
  );
}

export default App;
